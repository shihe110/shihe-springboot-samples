server:
  port: 8082

spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true # false默认表示使用消息队列，true表示使用发布订阅模式

mytopic: boot-activemq-topic # 目标消息队列名称